TODO for multiple browser support:

** FRAMES
- Abstract current AllFramesProcessor used in FramesCollection away
- FramesCollection constructor should not depend on mshtml.HTMLDocument

** IE specific Constraint classes (TableRowAttributeConstraint, LabelTextConstraint, ProximityTextConstraint)
- Move to InternetExplorer.Constraints namespace
- remove from Find class and expose them using extension methods on Find
- See if some/all can be changed to work with FireFox. If so revert above changes for these Constraints

** IElementCollection
DONE JVM - property IElementCollection.Elements should return object

** Document class
DONE JVM - Depends heavily on mshtml.HtmlDocument. These dependencies should be removed or be moved to browser specific implementation(s).

** DomContainer class
DONE JVM - Depends on mshtml.HtmlDocument because its an intermediar to Document. Might be solved if Document is changed.

** Element class
DONE JVM - Remove HTMLElement property and change all code depending on it.

** ElementAttributeBag
DONE JVM - Reimplement style support in GetValue(attributename)

** Document
DONE JVM - Remove dependencies on IHTMLDocument2
DONE JVM - Remove dependencies on IHTMLElement for attributes values from Body element
- Move GetPropertyValue to nativeBrowser class? Is Com interop specific

** Style
DONE JVM - Remove depenency on IHTMLStyle 

** Form
DONE JVM - Move Submit to NativeElement

** Move IE specific classes to specific folder(s) and namespace(s)

** Option
DONE JVM - Handle selected with NativeElement

** RadioCheck
DONE JVM - Move checked to NativeElement

** Table, TableBody, TableRow
- Multiple issues

** TextField
DONE JVM - remove dependencies in wrappers around TextArea or input text 

** Check for and replace 
-  new IEElement by DomContainer.NativeBrowser.CreateElement

*** FIREFOX
- Wrapp WaitForComplete implementation and override DomContainer methods
- move RunScript to FFBrowser
- move Eval to FFBrowser (and IEBrowser)
- what to do with Settings.CloseExistingBrowserInstances
** Changes to test suite
- Make sure none browser specific tests are executed for IE and FireFox
- other option would be to create a in memory browser (mock) to run the general test against and 
  test the browser specific classes seperately (will speed up the test suite, but it
  is possible some integration issues will be missed....)
- Cleanup tests who make use of IEHtmlInjector (use javascript with timer instead).

** Interfaces
- Add interfaces to all elements and elementcollections (like in the current 2.0 CTP). Mainly to provide a way to mock out WatiN in testing tools using Watin (like stormwind)



